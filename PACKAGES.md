# Packages

| Registry | Status |
|----------|--------|
| **npm** | `@beig-rules/jailbreak-machine` (docs + host scripts) |
| **Containers** | Not supported — requires macOS + Apple Virtualization.framework on Apple Silicon host |
| Maven / NuGet / RubyGems | Not applicable |

```bash
npm install @beig-rules/jailbreak-machine
```

Actual VM create/launch still runs on a Mac host per README (not inside Docker).
